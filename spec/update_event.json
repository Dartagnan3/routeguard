{
  "type": "UpdateEvent",
  "required": ["event_id", "claim_id", "previous_hash", "new_hash", "timestamp", "reason", "actor"],
  "properties": {
    "event_id": { "type": "string" },
    "claim_id": { "type": "string" },
    "previous_hash": { "type": "string" },
    "new_hash": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "reason": { "type": "string" },
    "actor": { "type": "string" }
  }
}
